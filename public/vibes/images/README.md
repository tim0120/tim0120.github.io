# vibes images

Drop image files here (.jpg .jpeg .png .gif .webp .avif).

They're picked up automatically at build time by `src/app/vibes/page.tsx`
and scattered into a collage by `src/components/VibesGallery.tsx` — no
manifest to maintain, just add or remove files.
